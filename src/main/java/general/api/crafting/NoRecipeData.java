package general.api.crafting;

/**
 * Payload used by recipe types that need only the standard resource fields and
 * processing duration supplied by {@link MachineRecipe}.
 */
public enum NoRecipeData {
	INSTANCE
}
